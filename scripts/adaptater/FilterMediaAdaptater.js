class FilterMediaAdapter {
    constructor(phtographer, media ) {
        this.Media = Media
        this.photographerId = this.photographerId
    }

    async filterByMeida() {
        return await FilterV2.filterByMedia(this.actor, this.Movies)
    }
}